# Host port for the chatbot web UI.
# The container still listens on port 8000 internally.
APP_PORT=8000

# Optional runtime configuration.
QWEN_MODEL=qwen3:4b
ENABLE_RETRIEVE_ENDPOINT=false
ENABLE_API_DOCS=false
